Study ID: CD-WORKLOAD-20260830-005
Module ID: CD005-SCHEMA-CONSTRAINED-ENRICHED-BIOGRAPHY-001
Path: P1 fresh reconstruction feasibility test

Task:

Using only the supplied independently frozen public authority evidence, reconstruct one bounded semantic relationship graph for the Robbie George Biography Plate™.

The graph must connect the Biography Plate to these nine relationship classes:

1. canonical biography
2. professional evidence
3. Naturepedia reference implementation
4. Robbie's Razor framework authorship
5. GC-MRD-v2.0 governing authority
6. Naturepedia Discovery Index
7. Naturepedia AI Catalog
8. x402 Pricing Manifest
9. Naturepedia MCP Server Card

Return exactly one valid JSON object and no surrounding commentary or markdown.

Schema and placement requirements:

1. Follow the separately supplied Study 005 Neutral Output Schema exactly.

2. Use exactly these root containers:

- subject
- relationships
- retrieval_rights_boundary

3. Use the exact field names, nesting, scalar types, and additional-properties restrictions declared by the supplied schema.

4. Return exactly nine relationship objects: one and only one object for each relationship class listed above.

5. Within each relationship object, keep:

- the relationship-class label in relationship;
- all public target facts in target; and
- one or more supporting source IDs or public authority URLs in provenance.

6. Do not relocate a value to another object or field. A value is not a substitute for correct local placement.

7. Populate every schema-required field from the supplied evidence. Do not leave placeholders, nulls, or empty strings.

Evidence and interpretation rules:

1. Use only the supplied evidence.

2. Preserve explicit identifiers, names, types, URLs, versions, status values, authorship, classifications, pricing values, network, asset, and remote transport facts when present.

3. Keep first-party authority claims distinct from independent validation. Do not convert first-party authorship, reference-implementation, or governing-authority statements into third-party empirical confirmation or universal validation.

4. Do not use the protected Enriched Query response, any paid payload, Study 004 model output, or external knowledge.

5. Do not claim that payment, HTTP success, route availability, or catalog registration proves semantic equivalence.

6. Do not invent reuse, training, embedding, ingestion, redistribution, resale, synchronization, dataset, commercial-implementation, or framework-implementation rights.

7. Do not summarize away the nine relationships. Each must remain separately machine-readable with its target and provenance.

8. Relationship array ordering is not important. Exact graph placement and field names are required.

9. Output must be parseable JSON.
