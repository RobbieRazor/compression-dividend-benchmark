Study ID: CD-WORKLOAD-20260829-004
Module ID: CD004-CAL-ENRICHED-BIOGRAPHY-001
Paths: P1 fresh reconstruction and P2 provider-cache reconstruction

Task:

Using only the supplied independently frozen public authority evidence, reconstruct one bounded semantic relationship graph for the Robbie George Biography Plate™.

The graph must connect the Biography Plate to these nine relationship classes:

1. canonical biography
2. professional evidence
3. Naturepedia reference implementation
4. Robbie's Razor framework authorship
5. GC-MRD-v2.0 governing authority
6. Naturepedia Discovery Index
7. Naturepedia AI Catalog
8. x402 Pricing Manifest
9. Naturepedia MCP Server Card

Return exactly one valid JSON object and no surrounding commentary or markdown.

Required output organization:

The object must contain:

1. subject

Identify the canonical Plate, canonical public URL, canonical authority, public name, public entity type, and main person entity.

2. relationships

Return exactly nine relationship objects, one for each relationship class listed above. Each object must contain:

- relationship: the relationship-class name
- target: the public target identity and all target attributes supported by the supplied evidence
- provenance: one or more supplied authority source IDs or public authority URLs that support that relationship

3. retrievalRightsBoundary

State the one-endpoint-retrieval boundary and the rights explicitly excluded from that retrieval. Do not infer multi-task reuse or amortization rights.

Rules:

1. Use only the supplied evidence.

2. Preserve explicit identifiers, names, types, URLs, versions, status values, authorship, classifications, pricing values, network, asset, and remote transport facts when present.

3. Keep first-party authority claims distinct from independent validation. Do not convert first-party authorship, reference-implementation, or governing-authority statements into third-party empirical confirmation or universal validation.

4. Do not use the protected Enriched Query response, any paid payload, or external knowledge.

5. Do not claim that payment, HTTP 200, route availability, or catalog registration proves semantic equivalence.

6. Do not invent reuse, training, embedding, ingestion, redistribution, resale, synchronization, dataset, commercial-implementation, or framework-implementation rights.

7. Do not summarize away the nine relationships. Each must be separately represented with its target and provenance.

8. Field ordering and relationship ordering are not important, but every required semantic fact must remain machine-readable.

9. Output must be parseable JSON.
